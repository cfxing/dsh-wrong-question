# 错题库插件 — 界面设计

## 气质与意象
一个"深夜书桌前认真温书"的场景。深色温和的渐变背景不是死黑，而是沉淀的墨蓝色；卡片像铺开的错题便笺，圆角克制、层级分明。重点信息（待复习题数、薄弱知识点）有明确的视觉锚点，不靠一堆彩色按钮堆砌。整体让人"想坐下来把题过完"的沉静感，而非工具栏般的生硬。

## 视觉策略
- 抛弃纯白高饱和配色，采用**低饱和墨蓝 + 细腻冷灰**为主调，品牌色（靛蓝 #4f46e5 → #6366f1）作为唯一强调色。
- 卡片用柔和的层级背景而非描边，减少"表格感"。圆角 14–18px，阴影轻而深（低透明度大模糊）。
- 图谱节点保留彩色分层，但统一为柔和色板，节点悬停/聚焦用品牌色描边。

## 配色方案
- 背景基底：#f7f8fb（页面）、#eceef4（次级）
- 卡片层级：#ffffff（浮层）叠加微弱阴影 rgba(15,23,42,.06)
- 文字：#1d2433（主）、#6b7280（次）、#9aa1b0（弱）
- 品牌强调：#4f46e5 / #6366f1（按钮、进度、活跃态）
- 语义：成功 #16a34a、警告 #d97706、危险 #dc2626

## 字体排版
- 与宿主 `--dsw-font-family` 一致；标题用较高字重（600/700），正文 600 字重。
- 数字类信息（题数、掌握率）用放大字号 + tabular-nums 增加稳定感。
- 强调 Respacing：标题 20/22px，卡片标题 15px，正文 13px，辅助 12px。

## 动效与交互
- 微动效仅用于关键反馈：tab 切换底部指示条用 0.18s ease 滑动、按钮 hover 提亮 0.15s、卡片 hover 上浮 2px + 阴影加深 0.2s。
- 图谱采纳现有自研 SVG（不能引第三方图库）：节点可拖拽、滚轮缩放、悬停高亮关联、边显示权重——保留，只优化配色与层级。
- 复习卡片是"一屏一道"的沉浸体验：图片居中、答案揭示用淡入，按钮是四宫格大按钮，点击即续到下一题。

## 设计禁忌
- 不用高饱和彩虹色做长条/大块背景；品牌色之外不做大面积纯色。
- 不用纯白 #fff 当页面大背景（改用 #f7f8fb）。
- 不用线条边框堆叠的"表格表"观感；优先层级背景 + 轻阴影。
- 不引入第三方图库（react-force-graph 等）——client bundle 无法打包 npm 依赖，图谱必须原生 SVG。